defaults:
  - base

n_positives: 1                                          # Use multiple positives
backbone_path: ${checkpoints.backbone_path}
gt_radius: ???
drop_last: false
per_env_batches: false

